#pragma once 
#include <vector>

using namespace std;










class Graph{

protected :
	//int x_size;
	//int y_size;

public :
	Graph(){}
	//Graph(int x, int y): x_size(x), y_size(y) {}
	vector<int> fusion(int x_size, int y_size);


};